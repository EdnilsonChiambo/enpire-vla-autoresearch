# LLM provider (required for researcher_agent)
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Backend overrides (optional; defaults come from config/default.yaml)
VLA_BACKEND=mock
ENV_BACKEND=pusht

# Kaggle T4: use openvla-4bit + simpler (maniskill3 branch)
# VLA_BACKEND=openvla-4bit
# ENV_BACKEND=simpler
# SIMPLER_BACKEND=maniskill3

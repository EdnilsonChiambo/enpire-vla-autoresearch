-r requirements.txt

# Kaggle T4 / Colab T4 — 4-bit OpenVLA (~6-7GB VRAM)
# OpenVLA-compatible stack: transformers 4.40.x needs tokenizers 0.19.x (not 0.15.x)
torch==2.3.1
torchvision==0.18.1
timm==0.9.10
tokenizers>=0.19,<0.20
accelerate==0.26.0
transformers==4.40.2
bitsandbytes>=0.43.0
sentencepiece>=0.2.0

# Optional: SIMPLER (install manually in notebook — see notebooks/kaggle_setup.ipynb)
# git clone https://github.com/simpler-env/SimplerEnv
# cd SimplerEnv && pip install -e .
